// src/features/gallery/tool/es-counter/utils/presets.ts
import { CountSettings } from '../types';

export const DEFAULT_SETTINGS: CountSettings = {
  newlineMode: '1',
  countSpaces: true,
  maxLength: null,
  counterUnit: 100,
};
